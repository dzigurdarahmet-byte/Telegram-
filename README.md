# 🤖 iiko + Claude AI — Telegram-бот аналитики ресторана

Готовый Telegram-бот, который подключается к вашей системе **iiko** и использует **Claude AI** для анализа данных ресторана на естественном языке.

## Что умеет бот

| Функция | Пример вопроса |
|---------|----------------|
| 📊 Выручка и средний чек | «Какая выручка за вчера?» |
| 🍽️ ABC-анализ блюд | «Какие блюда продаются хуже всего?» |
| 🚫 Стоп-лист | «Что сейчас в стоп-листе?» |
| 📦 Остатки на складе | «Какие остатки по бару?» |
| 👨‍🍳 Производительность сотрудников | «Кто из официантов работал лучше?» |
| ☀️ Автоотчёты | Утренний и вечерний отчёт в Telegram |

Бот понимает вопросы на обычном русском языке — не нужно учить команды.

---

## Быстрый старт (15 минут)

### Шаг 1. Получите 3 ключа

#### 1.1 Telegram Bot Token
1. Откройте Telegram и найдите `@BotFather`
2. Отправьте `/newbot`
3. Придумайте имя бота (например: «Аналитика Ресторана»)
4. Придумайте username (например: `my_resto_analytics_bot`)
5. Скопируйте токен — он выглядит так: `7123456789:AAH1234567890abcdef...`

#### 1.2 iiko API Login
У вас он уже есть! Это строка из iikoWeb → Настройки → API.

#### 1.3 Anthropic API Key (Claude)
1. Зайдите на https://console.anthropic.com
2. Зарегистрируйтесь (если ещё нет аккаунта)
3. Перейдите в API Keys → Create Key
4. Скопируйте ключ — он начинается с `sk-ant-...`
5. Пополните баланс (от $5 — хватит на ~месяц активного использования)

### Шаг 2. Узнайте свой Telegram ID

1. Найдите в Telegram бота `@userinfobot`
2. Напишите ему любое сообщение
3. Он ответит вашим ID (число вроде `123456789`)
4. Запишите — он нужен для ограничения доступа и автоотчётов

### Шаг 3. Установите бота на сервер

#### Вариант A: Docker (рекомендуется)

Вам нужен сервер с Docker. Подойдёт любой VPS от 500₽/мес (Timeweb, Selectel, Reg.ru).

```bash
# 1. Подключитесь к серверу по SSH
ssh root@ваш_сервер

# 2. Установите Docker (если нет)
curl -fsSL https://get.docker.com | sh

# 3. Скачайте файлы бота (или скопируйте через SCP)
mkdir -p /opt/iiko-bot
cd /opt/iiko-bot

# 4. Создайте файл .env (скопируйте из .env.example)
cat > .env << 'EOF'
TELEGRAM_BOT_TOKEN=7123456789:ваш_токен
IIKO_API_LOGIN=ваш_api_логин
ANTHROPIC_API_KEY=sk-ant-ваш_ключ
ALLOWED_USERS=123456789
ADMIN_CHAT_ID=123456789
EOF

# 5. Запустите
docker compose up -d --build

# 6. Проверьте логи
docker compose logs -f
```

#### Вариант B: Без Docker (Python напрямую)

```bash
# 1. Установите Python 3.11+
sudo apt update && sudo apt install -y python3.11 python3.11-venv

# 2. Создайте виртуальное окружение
cd /opt/iiko-bot
python3.11 -m venv venv
source venv/bin/activate

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Создайте .env (см. выше)

# 5. Запустите
python bot.py
```

Чтобы бот работал в фоне, используйте `systemd`:

```bash
cat > /etc/systemd/system/iiko-bot.service << 'EOF'
[Unit]
Description=iiko Claude Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/iiko-bot
ExecStart=/opt/iiko-bot/venv/bin/python bot.py
Restart=always
RestartSec=10
EnvironmentFile=/opt/iiko-bot/.env

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable --now iiko-bot
systemctl status iiko-bot
```

### Шаг 4. Проверьте работу

1. Откройте Telegram и найдите вашего бота
2. Нажмите `/start`
3. Попробуйте: `/today` или напишите «Какая выручка за вчера?»

---

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и список команд |
| `/today` | Сводка за сегодня |
| `/yesterday` | Сводка за вчера |
| `/week` | Отчёт за неделю |
| `/month` | Отчёт за месяц |
| `/stop` | Текущий стоп-лист |
| `/menu` | Информация по меню |
| `/staff` | Отчёт по сотрудникам |
| `/abc` | ABC-анализ блюд за месяц |

Или просто напишите вопрос своими словами!

---

## Архитектура

```
Telegram ←→ Python-бот ←→ iiko Cloud API (данные)
                ↕
          Claude AI (анализ)
```

Файлы проекта:

| Файл | Назначение |
|------|------------|
| `bot.py` | Telegram-бот, обработка команд и сообщений |
| `iiko_client.py` | Клиент iiko Cloud API (iikoTransport) |
| `claude_analytics.py` | Аналитика через Claude AI |
| `config.py` | Загрузка конфигурации из .env |
| `.env.example` | Шаблон конфигурации |
| `Dockerfile` | Сборка Docker-образа |
| `docker-compose.yml` | Запуск через Docker Compose |
| `requirements.txt` | Python-зависимости |

---

## Настройка автоотчётов

Бот может автоматически присылать отчёты:
- **08:00** — утренний брифинг (итоги вчера + стоп-лист)
- **22:00** — вечерний итог дня

Для этого заполните `ADMIN_CHAT_ID` в `.env`.

Время указано в UTC. Если ваш часовой пояс MSK (UTC+3), измените время в `bot.py`:
- Утренний: `"05:00"` (08:00 MSK)
- Вечерний: `"19:00"` (22:00 MSK)

---

## Стоимость использования

| Сервис | Стоимость |
|--------|-----------|
| iiko Cloud API | Бесплатно (входит в лицензию iiko) |
| Claude API | ~$5-15/мес при активном использовании |
| Telegram Bot | Бесплатно |
| VPS-сервер | 500-1000₽/мес |

---

## Решение проблем

**Бот не отвечает:**
- Проверьте логи: `docker compose logs -f`
- Убедитесь, что токен Telegram правильный

**«Организации не найдены»:**
- Проверьте API-логин iiko в iikoWeb
- Убедитесь, что лицензия API активна

**«Claude API ошибка»:**
- Проверьте ключ Anthropic
- Проверьте баланс на console.anthropic.com

**Нет данных за период:**
- Убедитесь, что в iiko есть закрытые заказы за этот период
- Некоторые отчёты доступны только с определённых версий iiko

---

## Дальнейшее развитие

Этот бот — MVP (первая версия). Следующие шаги:

1. **Вебхуки** — мгновенные уведомления при изменении стоп-листа
2. **Графики** — отправка графиков выручки в Telegram
3. **Прогнозирование** — Claude анализирует тренды и прогнозирует спрос
4. **Автозаказ** — при низких остатках автоматическое формирование заказа поставщику
5. **Мультиточки** — поддержка сети ресторанов
